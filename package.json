{
  "name": "learnmap",
  "private": true,
  "workspaces": [
    "api"
  ],
  "scripts": {
    "build:api": "npm run build -w api",
    "lint:api": "npm run lint -w api",
    "start:api": "npm run start:dev -w api"
  }
}
